# 🚀 Быстрый старт / Настройка

## 📋 Что нужно сделать перед использованием

### Шаг 1: Регистрация на K2Think.ai

1. Перейдите на сайт: https://www.k2think.ai/
2. Зарегистрируйтесь или войдите в свой аккаунт
3. Убедитесь, что вы можете использовать чат (попробуйте задать вопрос)

### Шаг 2: Установка расширения для браузера

Установите расширение **EditThisCookie (V3)** для вашего браузера:

- **Chrome**: https://chromewebstore.google.com/detail/editthiscookie-v3/ojfebgpkimhlhcblbalbfjblapadhbol
- **Firefox**: https://addons.mozilla.org/en-US/firefox/addon/edit-this-cookie/
- **Edge**: https://microsoftedge.microsoft.com/addons/detail/edit-this-cookie/fngmhnnpilhplaeedifhccceomclgfbg

### Шаг 3: Получение cookies

1. Зайдите на https://www.k2think.ai/ и авторизуйтесь
2. Нажмите на иконку расширения **EditThisCookie** в браузере
3. Нажмите кнопку **Export** (Экспорт)
4. Сохраните файл как `Cookie.json` в корне проекта

### Шаг 4: Конвертация cookies

1. Откройте терминал в папке проекта
2. Запустите конвертер:

```bash
npm run cookies
```

Или напрямую:
```bash
node src/cookie-converter.js
```

3. Конвертер автоматически:
   - Прочитает `Cookie.json`
   - Создаст `cookies.txt`
   - Сгенерирует скрипт `set-cookies.sh`

### Шаг 5: Настройка окружения

Выполните сгенерированный скрипт:

```bash
source set-cookies.sh
```

Или установите переменную окружения вручную:
```bash
export K2THINK_COOKIES="ваши_сконвертированные_cookies"
```

### Шаг 6: Проверка работы

Теперь библиотека готова к использованию!

```bash
# Проверка базового функционала
npm run demo

# Проверка диалога
npm run dialog

# Запуск тестов
npm test
```

## 🔧 Альтернативные способы

### Способ 1: Через переменную окружения

Если у вас уже есть cookies в формате строки:
```bash
export K2THINK_COOKIES="token=ваш_токен; session_id=ваша_сессия"
```

### Способ 2: Через файл cookies.txt

Создайте файл `cookies.txt` с содержимым:
```
token=ваш_токен
session_id=ваша_сессия
```

Библиотека автоматически прочитает этот файл.

## ⚠️ Важные замечания

- **Безопасность**: Никогда не делитесь своими cookies!
- **Срок действия**: Cookies могут истекать, при ошибках аутентификации обновите их
- **Приватность**: Файлы с cookies добавлены в `.gitignore` и не попадут в репозиторий

## 🐛 Частые проблемы

### Ошибка "Cookies не найдены"
- Убедитесь, что вы выполнили `source set-cookies.sh`
- Проверьте, что переменная `K2THINK_COOKIES` установлена: `echo $K2THINK_COOKIES`

### Ошибка "Аутентификация не удалась"
- Обновите cookies: повторите шаги 2-4
- Убедитесь, что вы авторизованы на k2think.ai

### Ошибка "Файл Cookie.json не найден"
- Убедитесь, что файл `Cookie.json` находится в корне проекта
- Проверьте расширение файла (должно быть `.json`, не `.txt`)

## 🎯 Готово!

После выполнения этих шагов вы можете использовать все возможности библиотеки:

```javascript
const { create } = require('k2think-api-builder');

// Создание API билдера
const builder = await create();

// Использование готовых шаблонов
const analyzer = builder.createFromTemplate('textAnalyzer');
const result = await analyzer.execute('Отличный продукт!');
console.log(result);
```

---

**🎉 Поздравляем! Вы готовы использовать k2think-api-builder!**
